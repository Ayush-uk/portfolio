"use client"; import {motion} from 'framer-motion'; import {Send,ArrowDownRight} from 'lucide-react'; import {personalInfo,contactItems} from '../data/userData';
type Props={onTabChange?:(tab:string)=>void;uiType?:'landing'|'modular'};
const Hero=({onTabChange,uiType}:Props)=>{const go=(e:React.MouseEvent<HTMLAnchorElement>,id:string)=>{e.preventDefault();if(uiType==='modular'&&onTabChange)return onTabChange(id);document.getElementById(id)?.scrollIntoView({behavior:'smooth'})};const socials=contactItems.filter(i=>['GitHub','LinkedIn','X','Discord'].includes(i.label));return <section id="home" className="relative z-10 flex min-h-screen items-center scroll-mt-20 px-5 py-28 sm:px-8 lg:px-12"><div className="section-shell grid items-center gap-14 lg:grid-cols-[1fr_360px] lg:gap-20"><div className="max-w-3xl"><motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="eyebrow flex items-center gap-3"><span className="size-2 rounded-full bg-[var(--success)]"/>{personalInfo.status}</motion.div><motion.h1 initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:.1}} className="mt-6 max-w-4xl text-balance text-[clamp(3.3rem,9vw,7.8rem)] font-bold leading-[.9] tracking-[-.075em]">Building digital <span className="text-[var(--primary)]">systems</span> with intent.</motion.h1><motion.p initial={{opacity:0}} animate={{opacity:1}} transition={{delay:.25}} className="mt-8 max-w-2xl text-lg leading-8 text-[var(--foreground-muted)] sm:text-xl">{personalInfo.aboutText}</motion.p><div className="mt-9 flex flex-wrap items-center gap-3"><a href="#projects" onClick={e=>go(e,'projects')} className="inline-flex items-center gap-3 bg-[var(--primary)] px-5 py-3 text-sm font-bold text-white transition hover:bg-[var(--primary-hover)]">View selected work <ArrowDownRight size={17}/></a><a href="#contact" onClick={e=>go(e,'contact')} className="inline-flex items-center gap-2 border border-[var(--border)] px-5 py-3 text-sm font-bold text-[var(--foreground-muted)] transition hover:border-[var(--primary)] hover:text-white">Start a conversation <Send size={16}/></a></div><div className="mt-10 flex items-center gap-5 border-t border-[var(--border)] pt-5"><span className="font-mono text-[10px] uppercase tracking-widest text-[var(--text-label)]">Currently working with</span><div className="flex gap-4">{socials.map(({icon:Icon,label,href})=><a key={label} href={href} target="_blank" rel="noreferrer" aria-label={label} className="text-[var(--foreground-subtle)] transition hover:text-white"><Icon size={18}/></a>)}</div></div></div><motion.div initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} transition={{delay:.2}} className="relative mx-auto w-full max-w-[360px]"><div className="absolute -left-3 -top-3 z-10 h-8 w-8 border-l-2 border-t-2 border-[var(--primary)]"/><div className="aspect-[4/5] overflow-hidden border border-[var(--border)] bg-[var(--surface)] p-2"><img src={personalInfo.avatarUrl} alt={personalInfo.name} className="h-full w-full object-cover grayscale transition duration-700 hover:grayscale-0"/></div><div className="mt-3 flex justify-between font-mono text-[10px] uppercase tracking-widest text-[var(--text-label)]"><span>AK / 001</span><span>Full stack developer</span></div></motion.div></div></section>}; export default Hero;
